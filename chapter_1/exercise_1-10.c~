#include <stdio.h>
int main(){
  int inp_cha;
  while((inp_cha=getchar()) != EOF){
    if(inp_cha == ' ') putchar("\b");
    else if(inp_cha == '\t') putchar("\t");
    else if(inp_cha == '\\') putchar("\\");
    else putchar(inp_cha);
  }
  return 0;
}
