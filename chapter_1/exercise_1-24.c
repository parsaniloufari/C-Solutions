#include <stdio.h>

int brace, brack, paranthese;

void in_qoute(int character);
void in_comment(void);
void search(int character);

int main() {
  int character;
  extern int brace, brack, paranthese;
  
  while((character=getchar())!=EOF){
    if(c == '/') {
      if((character=getchar())=='*'){
    }
  }
}